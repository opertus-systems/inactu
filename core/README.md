# Core

Security-sensitive Rust crates belong here.

v0 intent:
- signing and verification primitives
- policy evaluation
- provenance and receipt validation

Current crates:
- `core/verifier` - hashing/signature verification helpers aligned to `spec/hashing.md`
